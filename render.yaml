services:
  - type: web
    name: certgen-backend
    runtime: node
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node --import tsx/esm src/index.ts
    envVars:
      - key: NODE_ENV
        value: production
